---
title: "Overview"
order: 7
section: "Simulation"
description: "What the simulation layer is, how a run reads, and where events and action plans come from."
---

Simulation is the IOC's what-if layer. It lets an operator rehearse a busy day or a deteriorating condition on the digital twin before it happens, and see where things would strain while there is still time to act. It is organized by persona: Crowd for guest experience, Traffic for operations, and Environment for marine. Whatever the type, every run shares the same spine: a trigger, a forecast of the effect, and a proposed response.

The point of the layer is lead time. A run surfaces the squeeze at +45 minutes, while the day can still be changed, rather than after guests are already waiting or a reef is already over capacity.

## How a run works

Every run follows the same rhythm: choose the event to simulate, set the parameters, choose an action plan, run it, read the forecast, then implement a plan. Reading any run is reading that chain: what is being put on the island, what it does to capacity, and what closes the gap.

## The page at a glance

The layout is the same across all three types; only the controls and metrics change. Three columns do the work:

- Left, the setup: the forecast type or phase, the event to simulate, the parameters, and the action plan to test, with Run Simulation to start.
- Center, the digital twin: the Shura Island twin carrying the visualization layer, where the run reads as a heatmap or a field so a problem shows as a place rather than a number.
- Right, the forecast: the forecasted outcome, the metrics and KPIs, and the recommended actions, each with an Implement control.

The same working area moves through three states, from setup to run to implement.

<SimState state="default" />

**Default, setup.** The scenario is built on the left, the twin is live, and the right shows current readings. Nothing is forecast yet.

<SimState state="running" />

**Running, the forecast.** The twin visualizes the run and the right panel fills in: the forecasted outcome, the metrics and KPIs, and the recommended actions, each ready to implement.

<SimState state="implement" />

**Implementation, acting.** An action is implemented and the forecast re-runs with it applied: the outcome and metrics shift, the chosen action is marked while the rest stay open. Behind that, Implement routes the dispatch to the relevant teams, moves the action into the live operations view, and re-anchors the forecast baseline to track it. This is the step that turns a what-if into an executed response.

## Where events and action plans come from

The IOC does not author events or action plans. They are modeled upstream in the specialist simulation tools and made available in the IOC as the library you select from. Pedestrian and human crowd events are modeled in MassMotion; vehicle and traffic events are modeled in PTV and Aimsun.

<SimSources />

**Reading it.** Everything left of the divider is authored upstream and handed to the IOC as a library. Everything right of it happens in the twin: you pick an event and plan, set the parameters, run, and the twin visualizes the scenario, especially crowd density and vehicle traffic, then surfaces the forecast and recommended actions. Implement routes to live operations. The twin runs and shows the scenario; it does not author it.

## The three simulations

- Crowd, the guest experience persona, scoped to the Marina. Forecasts where guests build up and the mobility response that keeps them moving.
- Traffic, the operations persona, scoped to the road network and fleet. Forecasts how vehicles, charging, and road capacity hold up under load, and where to rebalance.
- Environment, the marine persona, scoped to North Reef. Forecasts how wind and conditions hit the reef and outdoor activity, and what to hold or move.
